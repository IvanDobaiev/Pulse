#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
<<<<<<<< HEAD:node_modules/.bin/user-home.ps1
    $input | & "$basedir/node$exe"  "$basedir/../user-home/cli.js" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../user-home/cli.js" $args
========
    $input | & "$basedir/node$exe"  "$basedir/../strip-bom/cli.js" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../strip-bom/cli.js" $args
>>>>>>>> 21f271e1498d521b63fe25521fe410c5ca21507a:node_modules/.bin/strip-bom.ps1
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
<<<<<<<< HEAD:node_modules/.bin/user-home.ps1
    $input | & "node$exe"  "$basedir/../user-home/cli.js" $args
  } else {
    & "node$exe"  "$basedir/../user-home/cli.js" $args
========
    $input | & "node$exe"  "$basedir/../strip-bom/cli.js" $args
  } else {
    & "node$exe"  "$basedir/../strip-bom/cli.js" $args
>>>>>>>> 21f271e1498d521b63fe25521fe410c5ca21507a:node_modules/.bin/strip-bom.ps1
  }
  $ret=$LASTEXITCODE
}
exit $ret
